/*
3.6
Write a program to sort a stack in ascending order (with the bigest items on top) you may use at most one additional stack to hold items but you may not copy the elements into any other data structure (such as an array) the stack suports the following operations: push, pop, peek, and isEmpty
*/
