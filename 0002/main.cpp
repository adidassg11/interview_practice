#include <stdio.h>
#include <vector>

//pretty numbers are multiples of primes, ugly are opposite
int findUgly(int n) {
    vector<int> v;
    v.push_back(0);
    v.push_back(1); 
    v.push_back(2);
    v.push_back(3); 
    
    int i = 0;
    while(i<n)
}

int main(void) {
    printf("in main \n");
    return 0;
}
